Hello This is Bhanu


Command
git  init -- create empty repo

git statu

git add <filename>  - to track,,, to give this command to update changes and ready for push

git commit -m
--- give master branch to finial 

git commit -m "Finish Change"

git remote add orign "Repo URL" --- we are makeing URL name to origin... now onwareds we can use orign insted of url

git push -u origin master